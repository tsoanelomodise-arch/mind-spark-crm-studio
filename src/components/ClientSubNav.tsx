import { Link, useRouterState } from "@tanstack/react-router";
import { ClientsIcon, LoginsIcon } from "@/components/ui/bespoke-icons";

export function ClientSubNav({ current }: { current?: "clients" | "logins" }) {
  return null;
}

